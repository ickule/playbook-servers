---
home:
  children:
    pc:
      hosts:
        localhost:
    servers:
      hosts:
        pi:

    laptops:
    desktops:
    vm:
      hosts:
        localhost:
    sbc:
      hosts:
        pi:
