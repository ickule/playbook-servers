---
home:
  children:
    pc:
      hosts:
        localhost:
    servers:
      hosts:
        aspire:

    laptops:
      hosts:
        aspire:
    desktops:
    vm:
      hosts:
        localhost:
    sbc:
